<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Light theme. Material 3 with no action bar, because each screen supplies
         its own MaterialToolbar. -->
    <style name="Base.Theme.SmartPantryManager" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/green_primary</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorPrimaryContainer">@color/green_surface</item>
        <item name="colorOnPrimaryContainer">@color/green_primary_dark</item>
        <item name="colorSecondary">@color/status_expiring</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorError">@color/status_expired</item>
        <item name="android:colorBackground">@color/surface_light</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>

    <style name="Theme.SmartPantryManager" parent="Base.Theme.SmartPantryManager" />

</resources>
